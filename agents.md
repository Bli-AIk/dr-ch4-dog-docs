# agents.md：文章工作流（计划 → 编写 → 审阅）

本文件记录本仓库（Kristal 中文教程）的文章生产流程，供 Claude Code 等 agent 遵循。

## 核心原则

- 一次只处理一篇文章（第 XX 集或番外 EXXX），不并行、不跳步。
- 每篇文章完成即 git 提交（**不推送**，等待用户润色；用户明确要求推送时才推）。
- **不写未经确认的内容**：事实（代码、文件、数值、历史）必须从源码 / git 历史 / 用户答复核实，绝不编造。
- **用户发来的草稿文本先润色再入文**：保持语气与内容，但顺滑句读、去冗余、补衔接——用户给的文字是素材，不是定稿。
- **教学注释优先**：这是教学文档——能用代码注释解释的，就写进注释（写详细：概念、原因、每步在干嘛）；正文保持精简，不重复注释已讲的内容。注释写给普通读者：用大白话，避免引擎内部术语（如 selected_wave、nil 索引）——读者没见过的概念换成日常说法。
- 用户润色后：用 `git diff` 学习用户的风格调整，并更新本文件与记忆。

## 流程

### 1. 计划（plan 模式）

- 进入计划模式；阅读对应大纲（`00-序章/`、`01-第一章-Dog-Battle/`、`05-番外/` 下同名文件）。
- 从真实源码 / git 历史核实关键事实（文章代码与 dr-ch4-dog 模组仓库一致）。
- 写出计划文件：文章结构（小节列表）、已核实事实清单、待确认问题。
- 用 AskUserQuestion 提出需要用户确认的问题；用户不回答时按推荐项执行（推荐项要标注）。

### 2. 编写

- 目标 **2000-3000 中文字符**（代码块不计入）；截图占位符格式 `> 📷 此处插入截图：xxx`；不使用 Obsidian 链接（`[[...]]`）。
- 代码块给**完整文件**（读者照抄能跑），英文注释下面加 `-- 翻译：` 行；行文上"先给引擎源码/上下文，再给我们的写法"。
- 每篇完成后 git 提交（分支名 `docs/episode-NN`，叠在上一集分支上）；提交信息 `docs: ...`。

### 3. 审阅

- 用户润色后，用 `git diff` 学习风格变化，更新本文件与记忆。
- 风格要点变化时，同步到下方"风格规范"。

## 风格规范（经多轮润色沉淀）

- **博客口语化**：直接问候读者（"欢迎回来！"）、口语连接词、允许自我调侃（"草"）、给读者授权（"适合你的就是最好的"）。
- **春秋笔法**：负面/吐槽委婉表达；正文保持克制，不点名批评。
- **诚实坦白**：只陈述事实；开发过程如实说（"第一版是抄的"）；不编造场景；代码/数值/链接与真实项目一致。
- **术语**：LÖVE（不叫 LÖVE2D）；Kristal 比喻"地基 + 引擎"；统一用"集"（不用"篇"）。
- **角色译名**：按模组 lang/names.json——克里斯（Kris）、苏西（Susie）、利艾尔斯（Ralsei）、诺艾尔（Noelle）、神烦狗（Annoying Dog）。
- **i18n API（kristal-i18n v0.2.0）**：`{key}` 内联引用是主写法（台词位置直接写 `{key}`，渲染时解析）；`Game:loc("key")` 是兜底——用于不支持 `{key}` 解析的地方（如存进代码变量的文本）。
- **hooks**：用 `HookSystem.hookScript` 挂接引擎类，不篡改引擎源码。
- **章节结构**：每集以"检查本集成果"收尾（对照第 03 集目标清单），并预告下一集。
- **mdbook 约束**：SUMMARY.md 的链接路径**不能含空格**（解析器限制），空格用 `%20` 编码；文件名本身保留空格。

## 发布

- push 到 main → GitHub Actions（`.github/workflows/pages.yml`）自动 mdbook 构建并部署 GitHub Pages。
- 站点：https://bli-aik.github.io/dr-ch4-dog-docs/
- 本地预览：`mdbook serve` / `mdbook build`。

## 事实来源

- 模组代码与 git 历史：dr-ch4-dog 仓库（本仓库是其 docs 子模块，代码在父仓库）。
- 引擎源码：Kristal v0.10.0 本地克隆（`src/engine/`）。
- 文章代码必须与 dr-ch4-dog 实际文件一致；教学简化需在文中说明。
